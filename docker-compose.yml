services:

  product-service:
    build: ./product-service-java/product-service
    ports:
      - "8080:8080"

  order-service:
    build: ./order-service-python
    ports:
      - "5000:5000"

  frontend:
    build: ./frontend/frontend
    ports:
      - "3000:3000"